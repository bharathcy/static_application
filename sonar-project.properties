sonar.projectKey=bharathcy_static_application
sonar.projectName=DevOps Profile Static Site

# Analyse the whole repo, but skip docs, CI, and Docker metadata
sonar.sources=.
sonar.exclusions=**/*.md,.github/**,Dockerfile,.dockerignore

# For SonarQube Cloud (SonarCloud) also set your org key, e.g.:
# sonar.organization=bharathcy
